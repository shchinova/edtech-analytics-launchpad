# Отчёт о качестве данных — RAW

**Источник данных:** Сырые данные (data/raw/*.csv)  
**Дата проверки:** 2026-04-11 12:39  
**Период данных:** январь 2024 — июнь 2025

## Объём данных

| Таблица | Строк |
|---|---|
| users | 5,000 |
| courses | 120 |
| enrollments | 10,732 |
| payments | 8,591 |
| payment_attempts | 12,408 |
| ad_costs | 1,641 |

## Сводка

| Уровень | Проверок с ошибками | Записей с ошибками |
|---|---|---|
| 🔴 CRITICAL | 5 | 410 |
| 🟡 WARNING  | 4 | 320 |
| **Итого** | **10** | **737** |

## Найденные ошибки

| Код | Таблица | Записей | Уровень | Описание | Рекомендация |
|---|---|---|---|---|---|
| `A1` | `users` | **100** | 🟡 WARNING | Дублирующиеся email у разных user_id — проблема для логина и рассылок. | Выяснить причину: общий ящик, повторная регистрация, ошибка импорта. |
| `A4` | `courses` | **5** | 🟡 WARNING | Курс с ценой 0 — если не бесплатный, ошибка заведения продукта. | Добавить флаг is_free; исключать из расчёта выручки и среднего чека. |
| `A5` | `courses` | **7** | 🟢 INFO | Курс не опубликован — не должен попадать в метрики конверсии и выручки. | Фильтровать WHERE is_published = 1 во всех витринах. |
| `A6` | `enrollments+users` | **164** | 🔴 CRITICAL | enrolled_at < registered_at — физически невозможно, ошибка ETL или часового пояса. | Исключать из воронки или корректировать до даты регистрации. |
| `A10` | `payments` | **40** | 🔴 CRITICAL | amount_rub < 0 — возвраты закодированы как отрицательные суммы. | Хранить возвраты отдельной записью с типом транзакции 'refund'. |
| `A11` | `payments` | **20** | 🔴 CRITICAL | amount_rub = 0 при status = success — ошибка интеграции со шлюзом. | Добавить NOT NULL и CHECK (amount_rub > 0) для статуса success. |
| `A12` | `payments+users` | **62** | 🔴 CRITICAL | paid_at < registered_at пользователя — физически невозможная ситуация. | Исключать из расчётов; проверить обработку часовых поясов в ETL. |
| `A15` | `payment_attempts→users` | **200** | 🟡 WARNING | user_id в payment_attempts не существует в users — гостевой чекаут или удалённые аккаунты. | Допустимо при гостевой оплате; исключать из пользовательских метрик. |
| `A16` | `payment_attempts→payments` | **124** | 🔴 CRITICAL | Успешная попытка оплаты без соответствующего платежа в payments. | Расследовать: webhook от шлюза мог не дойти — платёж списан, но не записан. |
| `A18` | `ad_costs` | **15** | 🟡 WARNING | spend_rub = 0 при impressions > 0 — данные о расходах потерялись при выгрузке. | Исключать из расчёта CPM и CAC; запросить повторную выгрузку. |

## Проверки без ошибок

| Код | Таблица | Уровень |
|---|---|---|
| `A2` | `users` | 🟡 WARNING |
| `A3` | `users` | 🟢 INFO |
| `A7` | `enrollments` | 🔴 CRITICAL |
| `A8` | `enrollments` | 🟡 WARNING |
| `A9` | `enrollments→courses` | 🔴 CRITICAL |
| `A13` | `payments+courses` | 🟡 WARNING |
| `A14` | `payments→enrollments` | 🔴 CRITICAL |
| `A17` | `payment_attempts` | 🔴 CRITICAL |
| `A19` | `ad_costs` | 🟡 WARNING |
| `A20` | `ad_costs` | 🟢 INFO |

## Приоритеты устранения

### 🔴 Немедленно (искажают ключевые метрики)

- **A6** enrolled_at < registered_at
- **A10** amount_rub < 0
- **A11** amount_rub = 0 при status = success
- **A12** paid_at < registered_at пользователя
- **A16** Успешная попытка оплаты без соответствующего платежа в payments.

### 🟡 В ближайший спринт

- **A1** Дублирующиеся email у разных user_id
- **A4** Курс с ценой 0
- **A15** user_id в payment_attempts не существует в users
- **A18** spend_rub = 0 при impressions > 0

### 🟢 Технический долг

- Добавить ENUM, NOT NULL и FOREIGN KEY constraints на уровне схемы БД
- Настроить автоматический запуск этого скрипта после каждого ETL-прогона

---
*Сгенерировано: `python scripts/check_quality.py --source raw`*